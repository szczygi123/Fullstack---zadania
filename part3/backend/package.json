{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../phonebook/phonebook && npm run build && shx cp -r dist ../../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"build release\" && git push"
  },
  "author": "Matti Luukkainen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
